# DCC-101v1 单轴步进电机控制系统 — 工程说明

## 1. 项目概述

本项目是基于 **TI MSPM0G3507** 微控制器的单轴闭环步进电机控制与视觉伺服系统。系统通过 UART 控制一台 **DCC-101v1** 闭环步进电机驱动器，同时接收 **K230 AI 视觉模组** 的误差反馈数据，实现闭环 PID 伺服控制。OLED 屏幕实时显示方向、角度、脉冲数、按键状态及 K230 反馈数据，四路实体按键支持手动点动调试。

- **开发环境**：TI Code Composer Studio (CCS), SysConfig
- **目标芯片**：MSPM0G3507（Cortex-M0+，80MHz）
- **编程语言**：C（TI DriverLib）
- **编码器分辨率**：16384 线/圈（360°）

---

## 2. 系统架构

```
┌──────────────┐    UART RX (误差帧)    ┌────────────────────┐
│  K230 视觉    │ ─────────────────────→ │  MSPM0G3507 主控    │
│  (AI模组)     │   AA 55 + dir/pulse   │                    │
└──────────────┘                        │  • 协议帧构造       │
                                         │  • PID 伺服控制    │
┌──────────────┐   UART2 TX (协议帧)    │  • OLED 实时显示   │
│ DCC-101v1    │ ←───────────────────── │  • 4路按键消抖     │
│ 步进电机      │   AA 55 + CMD + DATA  │                    │
└──────────────┘                        └────────────────────┘
                                         │
                                ┌───────┴───────┐
                                │ SSD1306 OLED  │
                                │  (I2C, 128x64)│
                                └───────────────┘
```

---

## 3. 目录结构

| 文件 / 目录 | 说明 |
|---|---|
| main.c | 主循环：OLED显示→按键处理→K230 PID伺服 |
| GC.h | 全局头文件汇总 |
| bujin.c / bujin.h | 电机控制（协议帧构造/UART发送）+ 4路按键消抖中断 |
| uart.c / uart.h | UART 发送 + K230 5字节误差帧接收 |
| key.c / key.h | 按键 GPIO 状态读取 |
| bihuan_pid.c / bihuan_pid.h | 增量式 PID 控制器 |
| oled.c / oled.h / oledfont.h | SSD1306 OLED 驱动（I2C） |
| delay.c / delay.h | 毫秒级延时 |
| empty.syscfg | SysConfig 外设配置（时钟/UART/GPIO/I2C/PWM） |
| bujin.txt | DCC-101v1 串口通信协议说明书（参考） |
| targetConfigs/ | 目标配置文件（MSPM0G3507.ccxml） |
| Debug/ | 编译输出目录 |

---

## 4. OLED 显示布局（128×64, 12号字体）

```
Dir:CW  Ang:   123    ← 方向 + 累计角度（度）
Pls:  5598          ← 累计脉冲数
Btn:FWD              ← 按键状态（FWD/REV/---）
FB:+1234             ← K230 反馈（无信号显示 -----）
```

每轮主循环刷新一次，实时反映电机状态。

---

## 5. 按键映射

| 按键 | GPIO | SysConfig 名称 | 功能 |
|---|---|---|---|
| 按键6 | PB9 | anjian6 | 电机**正向**旋转（30°/步） |
| 按键7 | PA16 | anjian7 | 电机**正向**旋转（30°/步） |
| 按键8 | PB8 | fan1 | 电机**反向**旋转（30°/步） |
| 按键5 | PA15 | fan2 | 电机**反向**旋转（30°/步） |

四路按键均通过 PWM 定时中断进行电平消抖（15周期滤波），同向按键并联触发。

---

## 6. 电机通信协议

通过 UART2（PA21 TX / PA22 RX）发送二进制协议帧控制 DCC-101v1 驱动器：

| 帧结构 | 帧头1 | 帧头2 | 设备ID | 指令码 | 数据长度 | 数据区 | 校验和 |
|---|---|---|---|---|---|---|---|
| **字节** | 1 | 1 | 1 | 1 | 1 | N | 1 |
| **值** | 0xAA | 0x55 | 0x01 | CMD | LEN | DATA | Checksum |

- 唤醒序列：3 字节 0xAA + 延迟 20ms
- 校验和：(ID + CMD + LEN + DATA 各字节) & 0xFF
- 波特率：115200 / 8N1

### 主要指令

| 指令码 | 名称 | 数据区 | 说明 |
|---|---|---|---|
| 0x11 | 相对旋转 | [方向, 0, 0, 脉冲L, 脉冲H] | 从当前位置旋转指定脉冲数 |
| 0x12 | 设原点 | 无 | 清零当前位置（保存到Flash） |
| 0x13 | 绝对旋转 | [方向, 脉冲L, 脉冲H] | 旋转到绝对坐标 |
| 0x14 | 设运行电流 | [电流值] | 最大运行电流（0~255） |
| 0x16 | 设用户方向 | [方向] | 反转逻辑方向 |

---

## 7. K230 视觉数据帧（5字节）

```
AA 55 [dir_x] [pulse_x_L] [pulse_x_H]
```

- dir_x：旋转方向（0x01=正，0x00=反）
- pulse_x：16位误差脉冲数（低字节在前）
- 帧头校验：首字节必须为 0xAA，次字节必须为 0x55

完整帧接收后置 `bujin_x=1`，主循环调用 PID 计算输出并发送 `dianji1_pulse()` 修正。

---

## 8. 外设引脚分配

| 外设 | 引脚 | 功能 |
|---|---|---|
| UART2 TX | PA21 | DCC-101v1 协议帧发送 |
| UART2 RX | PA22 | （预留） |
| UART3 TX（K230） | PA26 | K230 通信 |
| UART3 RX（K230） | PA13 | K230 视觉误差帧接收 |
| I2C SCL | PB2 | OLED |
| I2C SDA | PB3 | OLED |
| PWM (TIMA0) | PA0 | 定时中断（按键消抖） |
| 按键6 (正向) | PB9 | 数字输入，下拉 |
| 按键7 (正向) | PA16 | 数字输入，下拉 |
| 按键8 (反向) | PB8 | 数字输入，下拉 |
| 按键5 (反向) | PA15 | 数字输入，下拉 |
| SWCLK | PA20 | 调试时钟 |
| SWDIO | PA19 | 调试数据 |

---

## 9. 编译与烧录

1. 用 **TI Code Composer Studio** 打开工程目录
2. 目标器件：**MSPM0G3507**，调试器：XDS-110
3. 在 CCS 中打开 `empty.syscfg` → 重新生成 `ti_msp_dl_config.c/h`
4. 编译 → Debug 会话烧录

---

## 10. 工作流程

1. **上电启动**：初始化外设 → OLED 就绪 → 直接进入主循环
2. **主循环**：每轮刷新 OLED 显示 → 检测按键 → 处理 K230 反馈
3. **手动模式**：按住正向按键（PB9/PA16）→ `dianji_roll(30°)` 持续正转；按住反向按键（PB8/PA15）→ `dianji_roll(-30°)` 持续反转
4. **视觉伺服**：K230 误差帧到达 → PID 增量计算 → `dianji1_pulse()` 修正位置

---

## 11. 依赖项

- **TI MSPM0 SDK 2.10** (DriverLib)
- **SysConfig**（外设初始化代码生成）
- **DCC-101v1 闭环步进电机驱动器** ×1
- **K230 AI 视觉模组**（目标追踪误差反馈）
- **SSD1306 128×64 OLED**（I2C）
- **LP-MSPM0G3507 LaunchPad**
